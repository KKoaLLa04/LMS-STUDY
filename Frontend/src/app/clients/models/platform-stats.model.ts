/** Shape returned by the backend (Backend/DTOs/PlatformStatsDto.cs). */
export interface PlatformStats {
  studentsCount: number;
  teachersCount: number;
  coursesCount: number;
  contentHours: number;
}
